/**
 * Focus trap for dialogs / sheets — Tab cycles within container.
 * Re-exported from UI so shared and native hosts share one implementation.
 */

export { useFocusTrap, getFocusableElements } from '@fixnow/ui'
